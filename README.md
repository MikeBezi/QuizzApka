2. Otwórz terminal w app.
3. Zainstaluj wymagane biblioteki:
   pip install -r requirements.txt
4. Uruchom serwer aplikacji:
   python server.py
5. Otwórz przeglądarkę i wejdź na:
   http://localhost:8000

## Ukrywanie quizów
W pliku `ignore_quizzes.txt` możesz wpisać nazwy plików `.json`, które mają być ukryte
na liście wyboru (po jednej nazwie na linię), np. `ai900.json`.

##Ważne!
>Jeżeli nie działa komenda "pip install -r requirements.txt", zainstaluj pythona!
>https://www.python.org/downloads/